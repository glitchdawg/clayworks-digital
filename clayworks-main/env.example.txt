# Next.js Environment for Strapi Integration
# Copy this to .env.local

# Middleware API URL
NEXT_PUBLIC_MIDDLEWARE_URL=http://localhost:8080

# API Key for middleware authentication
STRAPI_API_KEY=development-api-key

# Public API key (exposed to browser for client-side fetches if needed)
NEXT_PUBLIC_API_KEY=development-api-key

# Preview mode secret
PREVIEW_SECRET=your-preview-secret-change-me

# Cache revalidation time in seconds
CACHE_REVALIDATE=60

# Strapi URL (for media URLs)
NEXT_PUBLIC_STRAPI_URL=http://localhost:1337
